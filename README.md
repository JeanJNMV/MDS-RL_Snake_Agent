Idea for the project: 
- compare algorithms (DQN, PPO, A2C, double DQN, etc.) with MLP and CNN policy networks
- compare reward shaping (sparse vs dense, etc.)
- compare state representations (full grid, local view, hand-crafted features, etc.)
- exploration of trees (BFS, DFS, MCTS, etc.)